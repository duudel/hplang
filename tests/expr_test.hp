
foo :: (x : s64, y : s64)
{
    return x + y;
}

main :: ()
{
    x == 5 - 7;
    y && a;
    60 + 7 - 8;
    2 * x - 7 / 16 + foo(x, y);
    a := 5;
    b : s64 = 2 * x;
    y := 0.0d - 10.0e5264f;
    15.5e-5f;
    a = b + 5;
    a = b = c + 5 * a;
    return 0;
}
