
foo :: (x : s64, y : s64)
{
    return x + y;
}

main :: ()
{
    x == 5 - 7;
    y && a;
    y && a;
    60 + 7 - 8;
    2 * x - 7 / 16 + foo(x, y);
    a = b + 5;
    a = b = c + 5 * a;
    return 0;
}
