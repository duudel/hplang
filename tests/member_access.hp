// Tests different struct member access expressions.
// 2016-04-30

my_struct :: struct
{
    x : f32;
    y : f32;
}

main :: ()
{
    s : string = "a string";
    size := s.size;
    data := s.data;

    s.size = 15;
    s.data = null;

    //m : my_struct;
    //m.x = 1.0f;
    //m.y = -3.04e2f;

    //a := m.y;

    //pm := &m;
    //pm.x *= 3.0f;
    //pm.y = m.x * pm.x - 2;

    //b := pm.y;
    return 0;
}
